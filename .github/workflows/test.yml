name: linkup
on:
  push:
    branches:
      - linkup
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@linkup
    - name: Publish to Registry
      uses: DriverTicks/Publish-Docker-Github-Action@linkup
      with:
        name: tickyuss/linkup
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
